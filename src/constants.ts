export const MIN_INFLATION_YEAR: number = 1913;
// TODO: Generate this dynamically as the current year
export const MAX_INFLATION_YEAR: number = 2026;
export const ICON_SIZE: number = 18;
export const SPINNER_SIZE: number = 32;
